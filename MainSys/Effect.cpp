#include "Effect.h"

void Effect::Load()
{
	//for(int i = 0; i < 9; i++)
	//{
	//	Image Anitmp = new Image;
	//	Anitmp.Load(_T("Dusty.bmp"), Rect(0 + (i * (33)), 0, 33 + (i * (33)), 35));
	//	KirbyStd.SetTransparentColor(RGB(255, 255, 255));

	//	Dusty.AddImage(Anitmp);
	//	Dusty.SetUpdateDelay(10);
	//}
}
void Effect::Update(DWORD tick, const Point& pt)
{
}
void Effect::Draw(HDC hdc)
{
}